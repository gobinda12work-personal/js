// filter(), map(), reduce() method in array 

// We use the filter() method to filter the array according to some certain condition and return it

// We use the map() method to update like add, substract, multiply the elements of the array accordings to what has been said into the call back function and return it

// We use the reduce() method to get the total number or total result of the elements of array according to the code what has been written inside the call back function and return it

// forEach() method can't return the value




